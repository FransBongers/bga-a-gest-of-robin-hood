interface LocationConfig {
  top: number;
  left: number;
  width: number;
  height: number;
}

interface TrackConfig {
  id: string | number;
  top: number;
  left: number;
  extraClasses?: string;
}
